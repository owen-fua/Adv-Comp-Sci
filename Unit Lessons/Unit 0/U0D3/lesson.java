public class lesson {
    public static void main(String[] args) {
        // variables
        int number = 5;


        // single = assigns a value
        // different by ==, which compares a value

        // different assignment operators
        number += 6; // this is the same as number = number + 6
        number -= 3; // number = number - 3
        number *= 10; // number = number * 10
        number /= 2; // number = number / 2
        
        // increment and decrement operators
        number += 1;
        number++;

        number -= 1;
        number--;

        // boolean operators, evaluate to true or false
        // use them in if statments
        
        // YOU CANNOT USE THESE FOR OBJECTS

        /* 
        number == 5; // equals
        number != 10; // not equals
        numer <= 15; // less than or equal to
        number >= 20 // greater then or equal to
        */



    }
}